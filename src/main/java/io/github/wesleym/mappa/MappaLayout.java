package io.github.wesleym.mappa;

/** High-level placement modes. {@link #AUTO} lets Mappa pick the best default for the map shape. */
public enum MappaLayout {
	AUTO,
	LAYERED,
	RADIAL,
	FORCE,
	GRID
}
